apiVersion: v1
kind: Service
metadata:
  labels:
    app: rancher-demo
  name: rancher-demo
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: rancher-demo
  sessionAffinity: None
  type: ClusterIP
